# Presets
#
# Config options:
# block: Minimum multiple for width/height (latent spatial block size for model, e.g. 8 or 64).
# target_mp: Target image area in megapixels (width * height / 1e6) for optimal generation.
# channels: Number of latent channels (usually 4 for Stable Diffusion models).
# min_ar: Minimum recommended aspect ratio (width/height) for this model.
# max_ar: Maximum recommended aspect ratio (width/height) for this model.
# search_range: how many block-steps to search around ideal height (int)
# rel_ar_tol: relative aspect-ratio tolerance (fraction, e.g. 0.001 = 0.1%)
# latent_scale: downscale factor between image px and latent spatial dims (usually 8)

SD1 (512px):
  block: 8
  target_mp: 0.262144
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  desc: "SD1.x, 512x512, 4-channel latent, block 8"

SD2 (768px):
  block: 8
  target_mp: 0.589824
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  desc: "SD2.x, 768x768, 4-channel latent, block 8"

SDXL (1024px):
  block: 8
  target_mp: 1.048576
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  desc: "SDXL, 1024x1024, 4-channel latent, block 8"

FLUX (1024px):
  block: 8
  target_mp: 1.048576
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  desc: "FLUX, 1024x1024, 4-channel latent, block 8"
