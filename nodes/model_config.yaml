# Presets
#
# Config options:
# block_size: The alignment constraint for pixel dimensions (e.g., must be a multiple of 8).
# spacial_downscale_ratio: The latent format's total downsampling factor. Pixel dimensions must be a multiple of this value.
# target_mp: The model's native training resolution in megapixels (e.g., 1024*1024 / 1e6 = 1.048576). The node optimizes resolution to be close to this area.
# min_ar: A practical guideline for the minimum aspect ratio to prevent poor composition. Not a hard technical limit.
# max_ar: A practical guideline for the maximum aspect ratio to prevent poor composition. Not a hard technical limit.
# search_range: An internal parameter for this node's search algorithm; defines how many block-steps to check around the ideal resolution.
# rel_ar_tol: An internal parameter for this node's search algorithm; the maximum allowed deviation from the target aspect ratio.

Custom:
  block_size: 8
  spacial_downscale_ratio: 8
  target_mp: 1.0
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.01
  desc: "Custom configuration defaults"

SD1 (512px):
  block_size: 8
  spacial_downscale_ratio: 8
  target_mp: 0.262144
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.001
  desc: "SD1.x, 512x512, block 8"

SD2 (768px):
  block_size: 8
  spacial_downscale_ratio: 8
  target_mp: 0.589824
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.001
  desc: "SD2.x, 768x768, block 8"

SDXL (1024px):
  block_size: 8
  spacial_downscale_ratio: 8
  target_mp: 1.0
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.01
  desc: "SDXL, 1024x1024, block 8"

SD3 (1024px):
  block_size: 8
  spacial_downscale_ratio: 8
  target_mp: 1.0
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.001
  desc: "SD3, 1024x1024, block 8"

FLUX.1 (1024px):
  block_size: 32
  spacial_downscale_ratio: 8
  target_mp: 1.0
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.001
  desc: "FLUX.1, 1024x1024, block 32"

FLUX.2 (1536px):
  block_size: 16
  spacial_downscale_ratio: 16
  target_mp: 2.25
  min_ar: 0.5
  max_ar: 3.75
  search_range: 10
  rel_ar_tol: 0.001
  desc: "FLUX.2, 1536x1536, block 16, VAE scale 16"
