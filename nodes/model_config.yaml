# Presets
#
# Config options:
# block: The VAE's total downsampling factor. Pixel dimensions must be a multiple of this value. For most SD models, this is 8.
# target_mp: The model's native training resolution in megapixels (e.g., 1024*1024 / 1e6 = 1.048576). The node optimizes resolution to be close to this area.
# channels: The number of channels in the latent space. This is 4 for nearly all Stable Diffusion models.
# min_ar: A practical guideline for the minimum aspect ratio to prevent poor composition. Not a hard technical limit.
# max_ar: A practical guideline for the maximum aspect ratio to prevent poor composition. Not a hard technical limit.
# search_range: An internal parameter for this node's search algorithm; defines how many block-steps to check around the ideal resolution.
# rel_ar_tol: An internal parameter for this node's search algorithm; the maximum allowed deviation from the target aspect ratio.
# latent_scale: The VAE downscale factor used to convert pixel dimensions to latent dimensions (e.g., latent_height = pixel_height / latent_scale). Usually 8.

SD1 (512px):
  block: 8
  target_mp: 0.262144
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  latent_scale: 8
  desc: "SD1.x, 512x512, 4-channel latent, block 8"

SD2 (768px):
  block: 8
  target_mp: 0.589824
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  latent_scale: 8
  desc: "SD2.x, 768x768, 4-channel latent, block 8"

SDXL (1024px):
  block: 8
  target_mp: 1.048576
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  latent_scale: 8
  desc: "SDXL, 1024x1024, 4-channel latent, block 8"

FLUX (1024px):
  block: 8
  target_mp: 1.048576
  channels: 4
  min_ar: 0.5
  max_ar: 2.0
  search_range: 5
  rel_ar_tol: 0.001
  latent_scale: 8
  desc: "FLUX, 1024x1024, 4-channel latent, block 8"
